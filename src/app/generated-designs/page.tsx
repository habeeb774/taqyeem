'use client';
import { useEffect,useState } from 'react';
import { DesignShell,designApi } from '@/components/design-shell';
export default function GeneratedPage(){const [items,setItems]=useState<any[]>([]),[loading,setLoading]=useState(true),[page,setPage]=useState(1),[total,setTotal]=useState(0),[error,setError]=useState('');useEffect(()=>{setLoading(true);designApi(`/api/generated-designs?page=${page}&limit=12`).then(r=>{setItems(r.designs);setTotal(r.total)}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[page]);const pages=Math.max(1,Math.ceil(total/12));return <DesignShell><main className="ds-main"><div className="ds-head"><div><h1>سجل التصاميم</h1><p>التصاميم الصادرة والمحفوظة داخل النظام.</p></div></div>{loading?<div className="ds-loading">جارٍ التحميل...</div>:error?<div className="ds-empty">{error}</div>:items.length?<div className="ds-history-grid">{items.map(x=><article className="ds-card" key={x.id}><div className="ds-history-img">{x.image_url?<img src={x.image_url} alt=""/>:<span>لا توجد معاينة</span>}</div><div className="ds-card-body"><h3>{x.template_name}</h3><div className="ds-meta"><span>المستخدم: {x.generated_by_name||'—'}</span><span>الموظف: {x.employee_name||'—'}</span><span>الصيغة: {String(x.image_format).toUpperCase()}</span><span>{x.width}×{x.height}</span><span>{new Date(x.created_at).toLocaleString('ar-SA')}</span></div>{x.image_url&&<div className="ds-actions"><a className="primary" href={x.image_url} download>تحميل</a></div>}</div></article>)}</div>:<div className="ds-empty">لا توجد تصاميم محفوظة بعد.</div>}<div className="ds-pages">{Array.from({length:pages},(_,i)=>i+1).map(p=><button className={p===page?'active':''} key={p} onClick={()=>setPage(p)}>{p}</button>)}</div></main></DesignShell>}
