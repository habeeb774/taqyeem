import {NextRequest,NextResponse} from 'next/server';import * as XLSX from 'xlsx';import {requireUser,jsonError,must} from '@/server/context';import {allVisibleEmployeeIds} from '@/db/queries/security';import {pool} from '@/db';
export async function GET(req:NextRequest){try{const c=await requireUser();must(c,'reports.view');const u=new URL(req.url),year=u.searchParams.get('year'),month=u.searchParams.get('month'),format=u.searchParams.get('format')||'json';const ids=await allVisibleEmployeeIds(c);if(!ids.length)return NextResponse.json({ok:true,performance:[],targets:[],attendance:[]});const [p,t,a]=await Promise.all([pool.query(`select e.employee_id,emp.employee_number,emp.full_name,c.year,c.month,e.final_score,e.result_label,e.status from public.evaluations e join public.employees emp on emp.id=e.employee_id join public.evaluation_cycles c on c.id=e.cycle_id where e.employee_id=any($1::uuid[]) and ($2::int is null or c.year=$2) and ($3::int is null or c.month=$3) order by c.year desc,c.month desc,emp.full_name`,[ids,year?Number(year):null,month?Number(month):null]),pool.query(`select t.employee_id,emp.employee_number,emp.full_name,c.year,c.month,t.target_amount,t.achieved_amount,case when t.target_amount>0 then round(t.achieved_amount/t.target_amount*100,2) else 0 end achievement_percentage from public.sales_targets t join public.employees emp on emp.id=t.employee_id join public.evaluation_cycles c on c.id=t.cycle_id where t.employee_id=any($1::uuid[]) and ($2::int is null or c.year=$2) and ($3::int is null or c.month=$3) order by c.year desc,c.month desc,emp.full_name`,[ids,year?Number(year):null,month?Number(month):null]),pool.query(`select a.employee_id,emp.employee_number,emp.full_name,c.year,c.month,a.base_score,a.final_score,a.status from public.attendance_evaluations a join public.employees emp on emp.id=a.employee_id join public.evaluation_cycles c on c.id=a.cycle_id where a.employee_id=any($1::uuid[]) and ($2::int is null or c.year=$2) and ($3::int is null or c.month=$3) order by c.year desc,c.month desc,emp.full_name`,[ids,year?Number(year):null,month?Number(month):null])]);if(format==='xlsx'){must(c,'reports.export');const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(p.rows),'الأداء');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(t.rows),'الأهداف');XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(a.rows),'الحضور');const buf=XLSX.write(wb,{type:'buffer',bookType:'xlsx'});return new NextResponse(buf,{headers:{'content-type':'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet','content-disposition':`attachment; filename="taqyeem-report-${year||'all'}-${month||'all'}.xlsx"`}});}return NextResponse.json({ok:true,performance:p.rows,targets:t.rows,attendance:a.rows});}catch(e){const x=jsonError(e);return NextResponse.json({ok:false,error:x.error},{status:x.status});}}
