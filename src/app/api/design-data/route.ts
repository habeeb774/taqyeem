import { NextResponse } from 'next/server';
import { pool } from '@/db';
import { visibleEmployees } from '@/db/queries/security';
import { jsonError, must, requireUser } from '@/server/context';

export async function GET(){try{const c=await requireUser();must(c,'design_templates.use');const employees=await visibleEmployees(c,{limit:200});const organizationScope=c.scopes.some(scope=>scope.type==='organization'),branchIds=c.scopes.filter(scope=>scope.type==='branch'&&scope.id).map(scope=>scope.id!),departmentIds=c.scopes.filter(scope=>scope.type==='department'&&scope.id).map(scope=>scope.id!),assigned=c.scopes.some(scope=>scope.type==='assigned_employees');const scopeSql=`($2::boolean or (cardinality($3::uuid[])>0 and e.branch_id=any($3::uuid[])) or (cardinality($4::uuid[])>0 and e.department_id=any($4::uuid[])) or ($5::boolean and exists(select 1 from public.evaluation_assignments a where a.employee_id=e.id and a.evaluator_user_id=$6::uuid)) or ($7::uuid is not null and (e.id=$7::uuid or e.manager_id=$7::uuid or e.supervisor_id=$7::uuid)))`;const args=[c.organizationId,organizationScope,branchIds,departmentIds,assigned,c.user.id,c.user.employeeId];const [d,b,j]=await Promise.all([pool.query(`select distinct d.id,d.name from public.departments d left join public.employees e on e.department_id=d.id and e.deleted_at is null where d.organization_id=$1::uuid and d.active=true and ($2::boolean or d.id=any($4::uuid[]) or exists(select 1 from public.employees e where e.department_id=d.id and e.organization_id=$1::uuid and e.deleted_at is null and ${scopeSql})) order by d.name`,args),pool.query(`select distinct b.id,b.name from public.branches b left join public.employees e on e.branch_id=b.id and e.deleted_at is null where b.organization_id=$1::uuid and b.active=true and ($2::boolean or b.id=any($3::uuid[]) or exists(select 1 from public.departments d where d.branch_id=b.id and d.id=any($4::uuid[])) or exists(select 1 from public.employees e where e.branch_id=b.id and e.organization_id=$1::uuid and e.deleted_at is null and ${scopeSql})) order by b.name`,args),pool.query(`select distinct j.id,j.name from public.job_titles j join public.employees e on e.job_title_id=j.id and e.deleted_at is null where j.organization_id=$1::uuid and e.organization_id=$1::uuid and j.active=true and ${scopeSql} order by j.name`,args)]);return NextResponse.json({ok:true,employees:employees.rows,departments:d.rows,branches:b.rows,job_titles:j.rows});}catch(e){const x=jsonError(e);return NextResponse.json({ok:false,error:x.error},{status:x.status});}}
