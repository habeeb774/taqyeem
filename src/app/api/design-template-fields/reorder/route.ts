import { NextResponse } from 'next/server';import { pool } from '@/db';import { jsonError,must,requireUser } from '@/server/context';import { z } from 'zod';
const schema=z.object({template_id:z.string().uuid(),field_ids:z.array(z.string().uuid()).min(1).refine(ids=>new Set(ids).size===ids.length,'Duplicate field ids are not allowed')});
export async function POST(req:Request){try{const c=await requireUser();must(c,'design_templates.manage_fields');const v=schema.parse(await req.json());const own=await pool.query(`select 1 from public.design_templates where id=$1::uuid and organization_id=$2::uuid and deleted_at is null`,[v.template_id,c.organizationId]);if(!own.rows[0])return NextResponse.json({ok:false,error:'NOT_FOUND'},{status:404});const client=await pool.connect();try{await client.query('begin');const fields=await client.query(`select id from public.design_template_fields where template_id=$1::uuid and id=any($2::uuid[]) for update`,[v.template_id,v.field_ids]);if(fields.rows.length!==v.field_ids.length)throw Object.assign(new Error('INVALID_FIELDS'),{status:400});for(const [i,id] of v.field_ids.entries())await client.query(`update public.design_template_fields set z_index=$1,sort_order=$1,updated_at=now() where id=$2::uuid and template_id=$3::uuid`,[i+1,id,v.template_id]);await client.query(`insert into public.audit_logs(organization_id,user_id,action,entity_type,entity_id,new_values) values($1::uuid,$2::uuid,'design_template_field.reorder','design_template',$3::uuid,$4::jsonb)`,[c.organizationId,c.user.id,v.template_id,JSON.stringify({field_ids:v.field_ids})]);await client.query('commit');}catch(e){await client.query('rollback');throw e;}finally{client.release();}return NextResponse.json({ok:true});}catch(e){const x=jsonError(e);return NextResponse.json({ok:false,error:x.error},{status:x.status});}}
